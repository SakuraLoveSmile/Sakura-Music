name: subsonic_api
description: A small, testable Dart client for the Subsonic REST API.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.11.1

dependencies:
  crypto: ^3.0.6
  dio: ^5.9.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.3
