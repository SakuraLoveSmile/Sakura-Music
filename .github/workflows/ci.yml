name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  analyze:
    name: Analyze
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Load read-only SSH deploy key (private feedback dependency)
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.FEEDBACK_DEPLOY_KEY }}

      - name: Install dependencies (root)
        run: flutter pub get

      - name: Run flutter analyze (root)
        run: flutter analyze

      - name: Install dependencies (subsonic_api)
        working-directory: packages/subsonic_api
        run: flutter pub get

      - name: Run flutter analyze (subsonic_api)
        working-directory: packages/subsonic_api
        run: flutter analyze

  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Load read-only SSH deploy key (private feedback dependency)
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.FEEDBACK_DEPLOY_KEY }}

      - name: Install dependencies (root)
        run: flutter pub get

      - name: Run flutter test (root)
        run: flutter test

      - name: Install dependencies (subsonic_api)
        working-directory: packages/subsonic_api
        run: flutter pub get

      - name: Run flutter test (subsonic_api)
        working-directory: packages/subsonic_api
        run: flutter test